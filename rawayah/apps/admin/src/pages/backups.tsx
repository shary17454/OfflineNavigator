export default function BackupsAdmin() {
  return (
    <main className="admin-shell">
      <h1>النسخ الاحتياطية والاستعادة</h1>
      <p>
        قرار مُتعمَّد: النسخ الاحتياطي والاستعادة عمليتان تشغيليتان حسّاستان (الاستعادة تحذف كل البيانات الحالية) —
        نُفِّذتا كسكربتات تشغَّل من طرف من يملك وصولاً مباشرًا للخادم، وليس كزر HTTP في لوحة الويب. تعريض عملية
        استعادة مدمِّرة أو ملفات نسخ احتياطية (تحتوي بيانات حساسة كهاش كلمات المرور) عبر HTTP يفتح سطح هجوم لا
        داعي له في هذه المرحلة.
      </p>

      <section>
        <h2>إنشاء نسخة احتياطية</h2>
        <pre>npm run db:backup</pre>
        <p>يحفظ نسخة مضغوطة بالتاريخ والوقت في مجلد <code>backups/</code> (غير مرفوعة على Git).</p>
      </section>

      <section>
        <h2>الاستعادة من نسخة احتياطية</h2>
        <pre>npm run db:restore -- backups/rawaya-&lt;التاريخ&gt;.sql.gz</pre>
        <p>يطلب تأكيدًا صريحًا قبل حذف البيانات الحالية واستبدالها. أضف <code>--yes</code> لتخطي التأكيد التفاعلي (سكربتات آلية فقط).</p>
      </section>

      <section>
        <h2>التوثيق الكامل</h2>
        <p>راجع <code>docs/BACKUP_RESTORE.md</code> للتفاصيل الكاملة والمخطط.</p>
      </section>
    </main>
  );
}
